# Research Retrieval Calibrator 项目总览

## 1. 项目使命

Research Retrieval Calibrator（RRC）是一个独立、轻量的跨学科文献检索校准智能体。它解决的不是“让大模型一次生成更漂亮的关键词”，而是验证并产品化以下命题：

> 用户对首轮候选至少给出 6 条明确反馈后，系统能否生成可追溯的查询变化，并让第二轮检索在真实问题上稳定优于第一轮。

首版不以知识图谱、长期记忆、Scheme 生成、复杂聊天界面或完整引用网络为目标。

## 2. 唯一产品主链

```text
用户研究意图
→ 最多 3 个高信息量澄清问题
→ 冻结 Research Intent IR
→ 直接交叉 / 问题域 / 方法域 / 桥接域四路线召回
→ 论文标准化、去重、向量化、重排和多样性筛选
→ 输出首轮 15-20 篇候选
→ 解析至少 6 条高度相关 / 部分相关 / 不相关反馈
→ 更新语义向量、术语权重和路线预算
→ 第二轮改写查询并重新检索
→ 合并两轮结果，输出最多 10 篇最终候选和检索演化报告
```

## 3. 不可突破的边界

- 不把 `adaptive-research-graph` 作为运行依赖。
- 不自动抓取知网页面，不模拟登录，不绕过验证码，不复用用户 Cookie。
- 不声称 arXiv 提供完整引用或被引网络。
- 不凭向量相似度自动合并疑似同文的中英文论文。
- 不从标题和摘要之外推断论文结论。
- 不让大模型虚构标题、作者、DOI、arXiv ID、URL 或检索结果。
- 不在双轮检索评测通过前扩建知识图谱、长期记忆、前端按钮系统或多智能体框架。
- 不为了凑满 10 篇而降低来源真实性或相关性标准。

## 4. 权威文件与冲突处理

按以下顺序解释项目要求：

1. 项目负责人在当前任务中的明确指令
2. 本文件 `agent.md`
3. `PRODUCT_SPEC.md`
4. `PROJECT_PLAN.md`
5. 当前 `docs/phases/M*-*.md`
6. `docs/architecture.md`、`docs/data_model.md`、`docs/evaluation.md`

如果文件冲突，停止实现并先修正文档基线；不得自行选择更方便的一版。

## 5. 唯一执行顺序

讨论稿同时给出了“平台接口优先”的编号和“核心风险优先”的落地顺序。为避免执行歧义，本仓库采用后者：

| 阶段 | 目标 | 进入下一阶段的硬门禁 |
|---|---|---|
| M0 | 冻结产品、数据、状态机与评测契约 | 契约测试全绿，10 题模板可校验 |
| M1 | 跑通 arXiv 首轮召回与真实元数据闭环 | 真实来源 ID 覆盖率 100%，元数据幻觉率 0 |
| M2 | 完成向量化、重排、证据槽位和首轮 15-20 篇选择 | 排序可复现，分数可解释，槽位规则有测试 |
| M3 | 完成反馈解析、查询校准和第二轮重检索 | 1 个真实问题上第二轮优于第一轮且全链可追溯 |
| M4 | 完成 10 个真实问题的离线评测 | 至少 7/10 改善，均值阈值与真实性门禁通过 |
| M5 | 接入 OpenAI 兼容服务并完成平台探测 | 流式/非流式、鉴权、状态恢复和平台探测通过 |
| M6 | 加入 CNKI 合规导入、双语展示和部署演示 | 导入、部署、回滚、演示与失败报告全部通过 |

阶段详情见 `docs/phases/README.md`。任何 Codex 会话一次只执行一个阶段中的一个原子任务。

## 6. Codex 开始任务前

1. 检查 `git status --short --branch`，不得覆盖用户未提交修改。
2. 阅读 `STATUS.md`，确认唯一活动阶段和任务 ID。
3. 阅读当前阶段文件及其依赖文档。
4. 从最新 `main` 创建 `agent/m<阶段>-t<任务>-<短名>` 分支。
5. 先写失败测试或失败验证，再写最小实现。
6. 保存红灯、绿灯、真实输入和环境信息等证据。
7. 只更新本任务直接影响的状态与文档。

## 7. 完成任务的定义

“代码已写完”不等于完成。一个任务只有同时满足以下条件才可完成：

- 任务范围内的测试先红后绿，且完整测试集通过。
- 静态检查、格式检查和类型检查通过。
- 真实外部集成证据与 mock 测试明确分开。
- 新增行为、失败模式和回滚方式已记录。
- `STATUS.md` 与阶段证据同步更新。
- 提交只包含本任务范围，提交消息符合 `docs/git_workflow.md`。
- 推送分支并创建或更新对应 PR；未经授权不直接修改远端 `main`。

## 8. 证据纪律

每个任务的机器可读证据保存在 `evaluation/reports/` 或当前阶段指定路径，并至少记录：

- 阶段和任务 ID
- Git commit
- UTC 时间
- Python 和依赖版本
- 执行命令
- 退出码
- 测试统计
- mock / recorded / real-network / human-judged / not-run 证据类型
- 输入数据哈希或稳定标识
- 失败原因与残余风险

不得用 mock 结果声称真实 arXiv、真实平台或真实 CNKI 流程通过。

## 9. 变更控制

- 产品主链、评测主指标、Go/No-Go 阈值和数据来源边界属于基线变更，必须独立提交。
- 在查看 M4 测试标签后，不得悄悄修改 Go 阈值；需要变更时创建新的评测版本。
- 禁止 `git reset --hard`、强制推送、重写已共享历史或提交密钥/缓存/模型权重。
- 新依赖必须说明用途、体积、许可证和是否影响离线运行。

## 10. 动态项目状态

当前活动阶段、当前任务、允许执行范围和阶段门禁，
均以 STATUS.md 为唯一权威来源。

开始任何任务前必须读取 STATUS.md；
agent.md 不重复维护动态阶段编号或任务进度。
